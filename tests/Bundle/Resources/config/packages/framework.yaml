framework:
  secret: automapper
  property_info:
    with_constructor_extractor: true
  test: ~
  serializer:
    enabled: true
  json_streamer:
    enabled: true
  profiler:
    enabled: "%kernel.debug%"
    only_exceptions: false